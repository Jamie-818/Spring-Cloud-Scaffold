# Eureka1 配置文件
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8762/eureka/,http://127.0.0.1:8763/eureka/ #向其他两个微服务注册
#    register-with-eureka: false #不显示在注册中心
spring:
  application:
    name: eureka1
server:
  port: 8761
